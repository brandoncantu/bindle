import React, { Component } from 'react';

class Footer extends Component {
    render() {
        return (
            <footer>
                <p>This projet was build with ReactJs, and building an API</p>
            </footer>
        );
    }
}

export default Footer;